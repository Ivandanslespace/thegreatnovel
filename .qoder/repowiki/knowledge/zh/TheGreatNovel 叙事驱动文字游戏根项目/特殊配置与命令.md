唯一游戏入口为 `python tools/turn_controller.py <存档路径>`，支持选项回合、自由输入、仅生成选项、记录叙述四种模式；新存档通过 `python tools/create_save.py` 基于 answers_new_game.yaml 与 templates/world_template.yaml 生成，并用 `python tools/validate_save.py` 校验。