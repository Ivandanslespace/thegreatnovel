- 所有状态变更必须经 Python 入口写入 SQLite 事务，禁止 LLM 直接编辑 saves/ 下的 YAML 或 Markdown 文件
- 展示给玩家的选项必须来自 pending_options 中 compile_options() 预验证的合同，legal==False 的候选不得出现
- SQLite 是唯一真相源，YAML 与 Markdown 仅为同步导出视图，冲突时以数据库为准
- 每回合通过 turn_controller 统一路由，LLM 只负责意图解析与小说叙述，不自行计算数值或修改状态
- 存档结构固定为 saves/{世界名}/ 下的一组 YAML + event_log.md + story.md + novel_draft.md + conversation_log.md + decision_audit.jsonl + campaign.sqlite3