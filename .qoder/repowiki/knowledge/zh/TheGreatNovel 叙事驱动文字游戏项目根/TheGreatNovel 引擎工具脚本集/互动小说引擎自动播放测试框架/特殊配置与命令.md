单局测试：`python tools/autoplay_test.py --save saves/锈铁方舟 --turns 50 --policy abc|random|aggressive|builder [--seed 42]`；批量测试：`python tools/autoplay_suite.py --turns 50 --policies abc,random,aggressive,builder --seed 42`。所有输出写入 `autoplay_runs/` 目录，测试前会自动复制存档到临时目录避免污染原始存档。